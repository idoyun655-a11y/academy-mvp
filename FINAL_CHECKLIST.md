# ✅ 최종 실행 검증 체크리스트

이 문서는 프로젝트가 실제로 실행 가능한 상태인지 확인하는 체크리스트입니다.

## 📋 설치 검증

### 1. 시스템 요구사항 확인

```bash
# Node.js 버전 확인 (v18 이상 필요)
node --version

# npm 버전 확인
npm --version
```

**예상 결과:**
```
v18.0.0 이상
npm 9.0.0 이상
```

### 2. 의존성 설치 확인

```bash
# 프로젝트 디렉토리로 이동
cd academy-system

# 의존성 설치
npm install
```

**예상 결과:**
```
added XXX packages in X.XXs
```

**문제 해결:**
```bash
# 캐시 삭제 후 재설치
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

## 🚀 서버 실행 검증

### 3. 개발 서버 시작

```bash
npm run dev
```

**예상 결과:**
```
> NODE_ENV=development tsx watch server/_core/index.ts
[시간] Server running on http://localhost:3000/
```

**포트 충돌 시:**
```bash
PORT=3001 npm run dev
```

### 4. 브라우저 접속 확인

1. 브라우저 열기
2. `http://localhost:3000` 접속
3. 로그인 페이지 표시 확인

**예상 화면:**
- 학원 관리 시스템 제목
- 이메일 입력 필드
- 비밀번호 입력 필드
- 로그인 버튼
- 테스트 계정 안내

## 🔐 로그인 검증

### 5. 관리자 로그인 테스트

```
이메일: admin@test.com
비밀번호: admin123
```

**예상 결과:**
- 로그인 성공
- 관리자 대시보드 표시
- 좌측 메뉴 표시 (학생관리, 반관리, 출결관리, 공지관리)

### 6. 학생 로그인 테스트

```
이메일: student1@test.com
비밀번호: student123
```

**예상 결과:**
- 로그인 성공
- 학생 홈 대시보드 표시
- 좌측 메뉴 표시 (홈, 시간표, 공지, 내정보)

## 📊 기능 검증

### 7. 관리자 기능 테스트

#### 7.1 학생 관리
```
1. 좌측 메뉴 → "학생 관리" 클릭
2. 학생 목록 표시 확인
3. "학생 추가" 버튼 클릭
4. 학생 정보 입력 (이름, 이메일, 전화번호)
5. 저장 버튼 클릭
6. 새 학생이 목록에 추가되었는지 확인
```

**예상 결과:**
```
✅ 학생 목록 조회 성공
✅ 학생 추가 성공
✅ 새 학생이 목록에 표시됨
```

#### 7.2 반 관리
```
1. 좌측 메뉴 → "반 관리" 클릭
2. 반 목록 표시 확인
3. "반 추가" 버튼 클릭
4. 반 정보 입력 (반 이름, 시간, 강사)
5. 저장 버튼 클릭
```

**예상 결과:**
```
✅ 반 목록 조회 성공
✅ 반 추가 성공
```

#### 7.3 출결 관리
```
1. 좌측 메뉴 → "출결 관리" 클릭
2. 반 선택
3. 날짜 선택
4. 학생별 출석 상태 기록 (출석/지각/결석)
5. 저장 버튼 클릭
```

**예상 결과:**
```
✅ 출결 조회 성공
✅ 출석 상태 기록 성공
```

#### 7.4 공지 관리
```
1. 좌측 메뉴 → "공지 관리" 클릭
2. 공지 목록 표시 확인
3. "공지 추가" 버튼 클릭
4. 제목과 내용 입력
5. 저장 버튼 클릭
```

**예상 결과:**
```
✅ 공지 목록 조회 성공
✅ 공지 추가 성공
```

### 8. 학생 기능 테스트

#### 8.1 홈 대시보드
```
1. student1@test.com으로 로그인
2. 홈 메뉴 클릭
3. 수강 중인 반 목록 확인
4. 최근 공지사항 확인
```

**예상 결과:**
```
✅ 홈 대시보드 표시
✅ 반 목록 표시
✅ 공지사항 표시
```

#### 8.2 시간표
```
1. 좌측 메뉴 → "시간표" 클릭
2. 주간 시간표 표시 확인
3. 수업 정보 확인
```

**예상 결과:**
```
✅ 시간표 조회 성공
✅ 수업 정보 표시
```

#### 8.3 공지사항
```
1. 좌측 메뉴 → "공지사항" 클릭
2. 공지 목록 표시 확인
3. 공지 클릭하여 상세 내용 확인
```

**예상 결과:**
```
✅ 공지 목록 조회 성공
✅ 공지 상세 보기 성공
```

## 🔍 API 검증

### 9. API 호출 확인

브라우저 개발자 도구 (F12) → Network 탭에서 확인:

```
GET /api/trpc/students.list?input=...
POST /api/trpc/students.create
PUT /api/trpc/students.update
DELETE /api/trpc/students.delete
```

**예상 결과:**
```
✅ 모든 요청이 200 또는 201 상태 코드 반환
✅ 응답에 데이터 포함
```

### 10. 콘솔 로그 확인

브라우저 콘솔 (F12 → Console)에서:

```javascript
// 에러가 없어야 함
// API 호출 로그 확인 가능
```

## 📝 로그아웃 검증

### 11. 로그아웃 테스트

```
1. 우측 상단 사용자 메뉴 클릭
2. "로그아웃" 클릭
3. 로그인 페이지로 이동 확인
```

**예상 결과:**
```
✅ 로그아웃 성공
✅ 로그인 페이지 표시
```

## 🎯 최종 체크

모든 항목이 완료되었는지 확인:

- [ ] Node.js v18 이상 설치됨
- [ ] npm install 완료
- [ ] npm run dev 실행 성공
- [ ] http://localhost:3000 접속 가능
- [ ] 로그인 페이지 표시됨
- [ ] 관리자 로그인 성공
- [ ] 학생 로그인 성공
- [ ] 학생 CRUD 동작
- [ ] 반 관리 동작
- [ ] 출결 관리 동작
- [ ] 공지 관리 동작
- [ ] 학생 홈 대시보드 표시
- [ ] 학생 시간표 표시
- [ ] 학생 공지 조회 성공
- [ ] 로그아웃 성공

## ✅ 완료!

모든 항목이 체크되면 프로젝트가 완전히 실행 가능한 상태입니다! 🎉

## 🐛 문제 발생 시

1. **에러 메시지 캡처** - 스크린샷 또는 텍스트
2. **브라우저 콘솔 확인** - F12 → Console 탭
3. **서버 로그 확인** - 터미널 출력
4. **포트 확인** - PORT=3001 npm run dev로 다른 포트 시도
5. **캐시 삭제** - npm cache clean --force

---

**마지막 업데이트**: 2024년 2월 12일
