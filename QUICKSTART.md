# 🚀 학원 관리 시스템 - 5분 안에 시작하기

## 1단계: 프로젝트 실행 (1분)

```bash
cd /home/ubuntu/academy-system
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

## 2단계: 로그인 (1분)

**관리자로 로그인:**
- 이메일: `admin@test.com`
- 비밀번호: `admin123`

**또는 학생으로 로그인:**
- 이메일: `student1@test.com`
- 비밀번호: `student123`

## 3단계: 기능 테스트 (3분)

### 관리자 기능 테스트

1. **학생 관리**
   - 좌측 메뉴 → "학생 관리"
   - 기존 학생 목록 확인
   - "학생 추가" 버튼 클릭하여 새 학생 등록
   - 학생 정보 수정/삭제 테스트

2. **반 관리**
   - 좌측 메뉴 → "반 관리"
   - 기존 반 목록 확인
   - "반 추가" 버튼으로 새 반 등록

3. **출결 관리**
   - 좌측 메뉴 → "출결 관리"
   - 반 선택 후 출석 현황 확인
   - 출석/지각/결석 기록

4. **공지 관리**
   - 좌측 메뉴 → "공지 관리"
   - 기존 공지 확인
   - "공지 추가"로 새 공지 등록

### 학생 기능 테스트

1. **로그아웃 후 학생 계정으로 로그인**
   - 우측 상단 사용자 메뉴 → "로그아웃"
   - `student1@test.com / student123`으로 로그인

2. **홈 대시보드**
   - 수강 중인 반 목록 확인
   - 최근 공지사항 확인

3. **시간표**
   - 좌측 메뉴 → "시간표"
   - 주간 수업 일정 확인

4. **공지사항**
   - 좌측 메뉴 → "공지사항"
   - 학원 공지 확인

## 🎯 주요 기능 요약

| 기능 | 관리자 | 학생 |
|------|--------|------|
| 학생 관리 | ✅ | ❌ |
| 반 관리 | ✅ | ❌ |
| 출결 관리 | ✅ | 조회만 |
| 공지 관리 | ✅ | 조회만 |
| 시간표 조회 | ✅ | ✅ |
| 알림톡 설정 | ✅ | ❌ |

## 💡 팁

- 콘솔에서 `F12`를 눌러 개발자 도구를 열면 API 호출 로그를 볼 수 있습니다
- 알림톡 발송 시 브라우저 콘솔에 로그가 출력됩니다
- 서버를 재시작하면 모든 데이터가 초기화됩니다 (더미 데이터 기반)

## 🔧 문제 해결

**포트 3000이 이미 사용 중인 경우:**
```bash
# 다른 포트에서 실행
PORT=3001 npm run dev
```

**의존성 설치 오류:**
```bash
# 캐시 삭제 후 재설치
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

---

더 자세한 내용은 `README_MVP.md`를 참고하세요.
