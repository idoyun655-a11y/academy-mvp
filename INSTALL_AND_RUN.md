# 📖 학원 관리 시스템 - 설치 및 실행 가이드

## 🖥️ 시스템 요구사항

- **Node.js**: v18 이상
- **npm** 또는 **pnpm**: 최신 버전
- **OS**: Windows, macOS, Linux

## 📥 설치 방법

### 1단계: 프로젝트 다운로드

```bash
# 프로젝트 디렉토리로 이동
cd academy-system
```

### 2단계: 의존성 설치

```bash
# npm 사용
npm install

# 또는 pnpm 사용 (더 빠름)
pnpm install
```

**설치 시간**: 약 2-3분

### 3단계: 환경 설정 (선택사항)

`.env.local` 파일이 자동으로 생성됩니다.
필요시 포트 등을 수정할 수 있습니다:

```bash
# .env.local 파일 편집
PORT=3000  # 기본값
JWT_SECRET=dev-secret-key-2024-local-dev
```

## 🚀 실행 방법

### 방법 1: npm 명령어 (권장)

```bash
npm run dev
```

### 방법 2: 시작 스크립트 (Linux/macOS)

```bash
./start-local.sh
```

### 방법 3: 직접 실행

```bash
npm run dev
```

## 🌐 브라우저에서 접속

서버 시작 후 브라우저에서 다음 주소로 접속합니다:

```
http://localhost:3000
```

## 🔐 로그인 정보

### 테스트 계정

| 역할 | 이메일 | 비밀번호 | 설명 |
|------|--------|---------|------|
| 관리자 | admin@test.com | admin123 | 모든 기능 접근 |
| 강사 | teacher@test.com | teacher123 | 관리자 기능 접근 |
| 학생1 | student1@test.com | student123 | 학생 기능만 |
| 학생2 | student2@test.com | student123 | 학생 기능만 |

### 로그인 방법

1. 로그인 페이지에서 위 계정 중 하나 선택
2. 이메일과 비밀번호 입력
3. "로그인" 버튼 클릭

## 🎯 기능 테스트

### 관리자 기능 테스트 (admin@test.com 로그인)

#### 1. 학생 관리
```
좌측 메뉴 → "학생 관리"
- 학생 목록 확인
- "학생 추가" 버튼으로 새 학생 등록
- 학생 정보 수정/삭제
```

#### 2. 반 관리
```
좌측 메뉴 → "반 관리"
- 반 목록 확인
- "반 추가"로 새 반 등록
- 반 정보 수정
```

#### 3. 출결 관리
```
좌측 메뉴 → "출결 관리"
- 반 선택
- 날짜 선택
- 출석/지각/결석 기록
```

#### 4. 공지 관리
```
좌측 메뉴 → "공지 관리"
- 공지 목록 확인
- "공지 추가"로 새 공지 등록
```

### 학생 기능 테스트 (student1@test.com 로그인)

#### 1. 홈 대시보드
```
좌측 메뉴 → "홈"
- 수강 중인 반 확인
- 최근 공지사항 확인
```

#### 2. 시간표
```
좌측 메뉴 → "시간표"
- 주간 수업 일정 확인
```

#### 3. 공지사항
```
좌측 메뉴 → "공지사항"
- 학원 공지 확인
```

#### 4. 내 정보
```
좌측 메뉴 → "내 정보"
- 개인 정보 확인
```

## 🔧 개발 명령어

```bash
# 개발 서버 실행
npm run dev

# 타입스크립트 체크
npm run check

# 프로덕션 빌드
npm run build

# 프로덕션 실행
npm start

# 테스트 실행
npm run test

# 코드 포맷팅
npm run format
```

## 🐛 문제 해결

### 포트 3000이 이미 사용 중인 경우

```bash
# 다른 포트에서 실행
PORT=3001 npm run dev
```

### 의존성 설치 오류

```bash
# 캐시 삭제 후 재설치
rm -rf node_modules package-lock.json
npm install
```

### 서버가 시작되지 않는 경우

```bash
# 1. Node.js 버전 확인
node --version  # v18 이상 필요

# 2. 포트 확인
netstat -an | grep 3000  # 포트 사용 여부 확인

# 3. 로그 확인
npm run dev  # 에러 메시지 확인
```

### 로그인이 안 되는 경우

1. 이메일과 비밀번호 정확히 입력했는지 확인
2. 브라우저 개발자 도구 (F12) → Console 탭에서 에러 메시지 확인
3. 서버 로그 확인

## 📊 프로젝트 구조

```
academy-system/
├── client/                  # 프론트엔드
│   ├── src/
│   │   ├── pages/          # 페이지 컴포넌트
│   │   ├── components/     # UI 컴포넌트
│   │   ├── lib/            # 유틸리티
│   │   └── App.tsx         # 메인 앱
│   └── package.json
│
├── server/                  # 백엔드
│   ├── routers.ts          # API 라우터
│   ├── auth.ts             # 인증 로직
│   └── _core/              # 프레임워크
│
├── drizzle/                # DB 스키마
├── shared/                 # 공유 코드
│
├── package.json            # 의존성
├── .env.local             # 환경 설정
├── tsconfig.json          # TypeScript 설정
└── README_MVP.md          # 기능 설명
```

## 💾 데이터 저장

**현재 상태**: 메모리 기반 더미 데이터
- ✅ 개발/테스트용으로 충분
- ⚠️ 서버 재시작 시 데이터 초기화
- ⏳ 프로덕션: 실제 DB 연동 필요

## 📚 추가 문서

- `README_MVP.md` - 기능 상세 설명
- `QUICKSTART.md` - 5분 안에 시작하기

## 🆘 지원

문제가 발생하면:

1. **브라우저 콘솔 확인** (F12 → Console)
2. **서버 로그 확인** (터미널 출력)
3. **테스트 계정 재확인**
4. **포트 충돌 확인** (PORT=3001 npm run dev)

## ✅ 체크리스트

설치 후 다음을 확인하세요:

- [ ] npm install 완료
- [ ] npm run dev 실행 성공
- [ ] http://localhost:3000 접속 가능
- [ ] 로그인 페이지 표시됨
- [ ] 테스트 계정으로 로그인 성공
- [ ] 관리자 메뉴 표시됨
- [ ] 학생 목록 조회 가능

모든 항목이 체크되면 설치가 완료된 것입니다! 🎉

---

**마지막 업데이트**: 2024년 2월 12일
